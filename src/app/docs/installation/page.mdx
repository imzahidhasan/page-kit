import PagekitTab from "@/components/site/pagekit-tab";

## Installation

Choose your framework to view the recommended steps.

<PagekitTab
  tabs={[
  {
    value: "nextjs",
    label: "Next.js",
    content: (
      <div className="space-y-4">
        <p>
          Install Page Kit alongside the tooling you need for a standard Next.js
          project.
        </p>
        <pre className="rounded-lg bg-muted px-4 py-3 text-sm">
          <code>{`npx create-next-app@latest my-app --typescript
cd my-app
npm install page-kit tailwindcss motion`}</code>
        </pre>
        <ol className="list-decimal space-y-2 pl-5 text-sm leading-6">
          <li>
            Run the generator to scaffold shadcn config:{" "}
            {`npx shadcn@latest init`}.
          </li>
          <li>
            Copy the contents of <code>src/components/site</code> into your
            project and update the import aliases to match your setup.
          </li>
          <li>
            Restart the dev server with <code>npm run dev</code> and verify the
            tabs render.
          </li>
        </ol>
      </div>
    ),
  },
  {
    value: "vite",
    label: "Vite + React",
    content: (
      <div className="space-y-4">
        <p>Set up a Vite project, then install the Page Kit dependencies.</p>
        <pre className="rounded-lg bg-muted px-4 py-3 text-sm">
          <code>{`npm create vite@latest my-app -- --template react-ts
cd my-app
npm install page-kit tailwindcss @radix-ui/react-tabs motion`}</code>
        </pre>
        <ol className="list-decimal space-y-2 pl-5 text-sm leading-6">
          <li>
            Run <code>npx tailwindcss init -p</code> and configure the content
            paths.
          </li>
          <li>
            Import <code>PagekitTab</code> in a page or component and pass a
            tabs array to render the MDX-friendly content.
          </li>
          <li>
            Start the dev server with <code>npm run dev</code>.
          </li>
        </ol>
      </div>
    ),
  },
  {
    value: "astro",
    label: "Astro",
    content: (
      <div className="space-y-4">
        <p>
          Astro works great with React islands. Install the React integration
          and pull in Page Kit components.
        </p>
        <pre className="rounded-lg bg-muted px-4 py-3 text-sm">
          <code>{`npm create astro@latest my-docs -- --template docs
cd my-docs
npm install @astrojs/react page-kit @radix-ui/react-tabs motion`}</code>
        </pre>
        <ol className="list-decimal space-y-2 pl-5 text-sm leading-6">
          <li>
            Enable the React integration: <code>npx astro add react</code>.
          </li>
          <li>
            Import <code>PagekitTab</code> into an Astro component using{" "}
           
          </li>
          <li>
            Run <code>npm run dev</code> and verify the tabbed installation
            guides.
          </li>
        </ol>
      </div>
    ),
  },
]}
  defaultValue="nextjs"
  ariaLabel="Framework installation tabs"
/>
